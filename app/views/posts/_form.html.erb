<div class = area>
  <%= form_with(model: @post,local: true, url: choose_new_or_edit) do |form| %>
  <p><%= form.label :"コメント"%></p>
  <p><%= form.text_field :content%></p>
  <p><%= form.submit%></p>
  <% end %>

</div>