<div class = "content_area">
  <h1>投稿一覧</h1>

  <ul>
    
    <% @posts.each do |post|%>

    <li class = timestamps><%= post.created_at%></li>
    <li class = content><%= post.content%></li>
    <%end%>
  </ul>
</div>